---
title: "R Notebook"
output: html_notebook
---

```{r}
library("readr")
library("dplyr")
library("ggplot2")
library("reticulate")
library("purrr")
library("reshape2")
library("stringr")
library("irlba")
theme_set(theme_bw())
```

```{r}
data_dir <- "/Users/kris/Desktop"
paths <- c(
  "/Users/kris/Documents/stability_outputs/vae90.yaml_2.tar.gz",
  "/Users/kris/Documents/stability_outputs/vae90.yaml_3.tar.gz"
)
result <- read_acts(paths, data_dir = data_dir)
```

```{r}
output_dirs <- sapply(paths, function(z) file.path(data_dir, basename(z)))
Xy <- read_csv(file.path(output_dirs[1], "Xy.csv"))
subsets <- lapply(output_dirs, function(s) read_csv(file.path(s, "vae90", "features", "subset.csv")))
```

```{r}
head(subsets[[1]] %>% arrange(path))
head(subsets[[2]] %>% arrange(path))
```

