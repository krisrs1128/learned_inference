organization:
  features_dir: "features/vae_boot/"
  out_dir: "runs/vae_boot/"
  train_dir: "train"
  xy: "Xy.csv"
train:
  checkpoint: null
  batch_size: 32
  lr: 0.001
  n_epochs: 2
  save_every: 1000
  z_dim: 64
bootstrap:
  path: "bootstraps/vae_boot/bootstraps.csv"
  B: 5
